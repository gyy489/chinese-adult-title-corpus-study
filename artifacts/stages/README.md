# Final stage manifests

These manifests describe the retained endpoint of each research stage. They are
not diaries of abandoned attempts. Counts are aggregate, paths point only to
public code or artifacts, and private inputs are named by role rather than by
filename or source identity.

Each manifest distinguishes what is reproducible here from what requires the
restricted corpus. The manifests themselves are included in the release
checksum inventory.
